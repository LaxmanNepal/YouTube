const DATA_URL='data/growth-update-2026-08-25.json';let data={topics:[]};let currentFilter='all';const $=s=>document.querySelector(s);async function load(){try{const r=await fetch(DATA_URL+'?v='+Date.now(),{cache:'no-store'});if(!r.ok)throw Error('Data unavailable');data=await r.json();render()}catch(e){$('#topics').innerHTML='<div class="glass topic" style="grid-column:1/-1"><h3>Growth data is not available yet.</h3><p>Research data could not be loaded.</p></div>';$('#updated').textContent='Data unavailable'}}function render(){const topics=(data.topics||[]).filter(t=>currentFilter==='all'||t.category===currentFilter);$('#updated').textContent='Updated '+formatDate(data.updated_at);$('#year').textContent=new Date().getFullYear();$('#heroStats').innerHTML=`<div class="stat"><b>${data.topics?.length||0}</b><span>OPPORTUNITIES</span></div><div class="stat"><b>${data.topics?.[0]?.seo_score||data.topics?.[0]?.score||'—'}</b><span>TOP SEO</span></div><div class="stat"><b>${data.research?.sources_count||'—'}</b><span>RESEARCH SIGNALS</span></div>`;$('#topics').innerHTML=topics.map(card).join('')}function slug(t){return String(t.slug||`topic-${t.rank||1}`).toLowerCase().replace(/[^a-z0-9-]+/g,'-').replace(/^-|-$/g,'')}function card(t){const rank=t.rank||data.topics.indexOf(t)+1;return `<article class="glass topic"><span class="rank">#${rank} · ${esc(t.category||'Tech')}</span><span class="score">${t.seo_score||t.score||0}/100</span><h3>${esc(t.topic)}</h3><p>${esc(t.why_make||'High-potential topic for the channel.')}</p><div class="chips"><span class="chip">Demand ${t.demand||'—'}</span><span class="chip">Competition ${t.competition||'—'}</span><span class="chip">Fit ${t.channel_fit||'—'}</span></div><a class="open" style="display:block;text-align:center;text-decoration:none" href="topics/${slug(t)}.html">Open complete strategy →</a></article>`}function formatDate(v){if(!v)return'Unknown';const d=new Date(v);return isNaN(d)?v:d.toLocaleString(undefined,{dateStyle:'medium',timeStyle:'short'})}function esc(v=''){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');currentFilter=b.dataset.filter;render()}));load();