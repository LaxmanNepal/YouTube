let data={topics:[]};let currentFilter='all';const $=s=>document.querySelector(s);const DATA_URL='data/growth-update-2026-08-25-refresh.json';async function load(){try{const r=await fetch(DATA_URL+'?v='+Date.now(),{cache:'no-store'});if(!r.ok)throw Error('Data unavailable');data=await r.json();render()}catch(e){if($('#topics'))$('#topics').innerHTML='<div class="glass topic" style="grid-column:1/-1"><h3>Growth data is not available yet.</h3><p>Research data could not be loaded.</p></div>';if($('#updated'))$('#updated').textContent='Data unavailable'}}function render(){const topics=(data.topics||[]).filter(t=>currentFilter==='all'||t.category===currentFilter);if($('#updated'))$('#updated').textContent='Updated '+formatDate(data.updated_at);if($('#year'))$('#year').textContent=new Date().getFullYear();if($('#heroStats'))$('#heroStats').innerHTML=`<div class="stat"><b>${data.topics?.length||0}</b><span>OPPORTUNITIES</span></div><div class="stat"><b>${data.topics?.[0]?.seo_score||data.topics?.[0]?.score||'—'}</b><span>TOP SEO</span></div><div class="stat"><b>${data.research?.sources_count??'—'}</b><span>RESEARCH SIGNALS</span></div>`;if($('#topics'))$('#topics').innerHTML=topics.map(card).join('')}function slug(t){return String(t.slug||`topic-${t.rank||1}`).toLowerCase().replace(/[^a-z0-9-]+/g,'-').replace(/^-|-$/g,'')}function card(t){const rank=t.rank||data.topics.indexOf(t)+1;return `<article class="glass topic"><span class="rank">#${rank} · ${esc(t.category||'Tech')} · ${esc(t.market||'')}</span><span class="score">${t.seo_score||t.score||0}/100</span><h3>${esc(t.topic)}</h3><p>${esc(t.why_make||'High-potential topic for the channel.')}</p><div class="chips"><span class="chip">${esc(t.audience||'Audience')}</span><span class="chip">Demand ${esc(t.demand||'—')}</span><span class="chip">Competition ${esc(t.competition||'—')}</span><span class="chip">Fit ${esc(t.channel_fit||'—')}</span></div><a class="open" style="display:block;text-align:center;text-decoration:none" href="topics/${slug(t)}.html">Open complete strategy →</a></article>`}function formatDate(v){if(!v)return'Unknown';const d=new Date(v);return isNaN(d)?v:d.toLocaleString(undefined,{dateStyle:'medium',timeStyle:'short'})}function esc(v=''){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');currentFilter=b.dataset.filter;render()}));load();