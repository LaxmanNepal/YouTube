name: Build Growth Pages and Archive
on:
  push:
    paths:
      - 'data/growth-update-*.json'
      - 'scripts/build_growth_pages.py'
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python scripts/build_growth_pages.py
      - name: Commit generated pages and archive
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add topics archive
          git diff --cached --quiet || git commit -m 'Auto-build dated topic pages and growth archive'
          git push
